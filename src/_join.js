/**
 * Join an array of strings using a given separator.
 *
 * @param {string[]} strings The input array of strings.
 * @param {string} sep The given separator.
 * @return string
 */
const _join = (strings, sep) => strings.join(sep);
export default _join;
